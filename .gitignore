*.exe
/server
/server.exe

config.json
.env
.env.generated
.env.recovery
.deploy.env
# Docker runtime env — holds DB/admin secrets; only .env.docker.example is tracked
.env.docker
.env.docker.local
# Personal Cosmos Cloud import (not part of the project's official deploy path)
docker-compose.cosmos.yml
dev-tools/

/web/static/react/
/node_modules/

# Nuxt UI frontend — ignore generated/installed artifacts, track source
/web/nuxt-ui/node_modules/
/web/nuxt-ui/.nuxt/
/web/nuxt-ui/.output/
/web/nuxt-ui/dist/

/thumbnails/
/uploads/
/logs/
/videos/
/music/
/hls_cache/

*.mp4
*.mkv
*.avi
*.mov
*.wmv
*.mp3
*.wav
*.flac
*.aac
*.ogg

.idea/
.vscode/
*.swp
*.swo
*~

.claude/
!.claude/commands/

.DS_Store
Thumbs.db

/issues/
/reports/
*.sarif
*.sarif.json

# local script backups (may contain secrets)
*.bak

/vendor
# package-lock.json tracked for reproducible builds — do not ignore
github-runner/setup-runner.sh
github-runner/.runner.env

# Cursor AI rules
.cursor/

# Runtime-created directories (not source)
/data/
/analytics/
/.remember/
# Claude session-memory notes (canonical store lives outside the repo); never commit
/memory/

# Runtime dirs accidentally created under source when app runs from wrong cwd
/internal/config/data/
/internal/config/logs/
/internal/config/temp/
/internal/config/analytics/
/internal/config/music/
/internal/config/playlists/
/internal/thumbnails/thumbnails/

# IDE problem reports (generated by JetBrains/Qodana inspections)
/problems/

/old

# Claude Design handoff artifacts — local design source-of-truth, not source.
/.handoff/
/Media\ Server\ Pro-handoff.zip

# Local workspace scratch — manifest helpers, env file backups
/build_manifest.js
*.env.backup.txt
*.env.backup

# Local Go workspace file — replace directive in go.mod covers the patched module
go.work
go.work.sum
ugreen_update_docker_deployed-DO-NOT-COMMIT-EVER.md
deploy-nas.sh
/overnight-audit
OVERNIGHT_REPORT.md
